import Screen from '../../components/Screen.jsx'

// Placeholder. The daily check-in (mental state + body pain + note) is Phase 2.
export default function Checkin() {
  return (
    <Screen title="Check-in">
      Hier komt je dagelijkse check-in: mentale staat, lichamelijke pijn en een
      korte notitie.
    </Screen>
  )
}
