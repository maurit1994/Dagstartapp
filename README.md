# Anker

Personal daily tool: tracking, ADHD support, thought capture, and daily check-ins.

Local-first: all data lives in `localStorage` on the device. No backend, no accounts.

## Getting started

```bash
npm install
npm run dev
```

Then open the printed `http://localhost:5173` URL.

See `CLAUDE.md` for architecture and conventions.
